# PyAgent Products & Tooling Architecture

> **Product Integrations, SDKs, and Tooling Ecosystem**  
> **Author:** Senior Cloud Architect  
> **Date:** February 2026

---

## 1. Product Landscape

### PyAgent in the Ecosystem

```
┌─────────────────────────────────────────────────────────────────────────┐
│                        AI/ML ECOSYSTEM                                   │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│  ┌─────────────────┐     ┌─────────────────┐     ┌─────────────────┐   │
│  │  INFRASTRUCTURE │     │   FRAMEWORKS    │     │   PLATFORMS     │   │
│  │                 │     │                 │     │                 │   │
│  │  • Azure OpenAI │     │  • LangChain    │     │  • Azure ML     │   │
│  │  • OpenAI API   │     │  • Semantic     │     │  • Databricks   │   │
│  │  • AWS Bedrock  │     │    Kernel       │     │  • Snowflake    │   │
│  │  • Google Vertex│     │  • LlamaIndex   │     │  • Fabric       │   │
│  │                 │     │  • Haystack     │     │                 │   │
│  └────────┬────────┘     └────────┬────────┘     └────────┬────────┘   │
│           │                       │                       │             │
│           └───────────────────────┼───────────────────────┘             │
│                                   │                                      │
│                           ┌───────┴───────┐                             │
│                           │               │                             │
│                           │   PYAGENT     │                             │
│                           │               │                             │
│                           │  Simple API   │                             │
│                           │  Enterprise   │                             │
│                           │  Features     │                             │
│                           │               │                             │
│                           └───────────────┘                             │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 2. Framework Integrations

### 2.1 LangChain Adapter

**Purpose:** Bridge PyAgent with LangChain ecosystem for tool sharing and chain integration.

```python
from pyagent.integrations import langchain_adapter

# Import LangChain tool into PyAgent
from langchain.tools import WikipediaQueryRun
wiki_tool = langchain_adapter.import_tool(WikipediaQueryRun())

# Export PyAgent agent for LangChain use
lc_agent = langchain_adapter.export_agent(my_pyagent)

# Import LangChain chain
my_chain = langchain_adapter.import_chain(lc_chain)
```

**Architecture:**

```
┌─────────────────┐         ┌─────────────────┐
│   LangChain     │         │    PyAgent      │
│                 │         │                 │
│  ┌───────────┐  │ import  │  ┌───────────┐  │
│  │   Tool    │──┼────────►│  │  Skill    │  │
│  └───────────┘  │         │  └───────────┘  │
│                 │         │                 │
│  ┌───────────┐  │ export  │  ┌───────────┐  │
│  │   Agent   │◄─┼────────┼──│  Agent    │  │
│  └───────────┘  │         │  └───────────┘  │
│                 │         │                 │
│  ┌───────────┐  │ bridge  │  ┌───────────┐  │
│  │   Chain   │◄─┼────────►│  │ Pipeline  │  │
│  └───────────┘  │         │  └───────────┘  │
└─────────────────┘         └─────────────────┘
```

### 2.2 Semantic Kernel Adapter

**Purpose:** Integration with Microsoft Semantic Kernel for enterprise AI orchestration.

```python
from pyagent.integrations import semantic_kernel_adapter

# Create SK kernel from PyAgent
kernel = semantic_kernel_adapter.create_kernel()

# PyAgent function as SK function
sk_func = semantic_kernel_adapter.create_kernel_function(my_agent)

# Import SK plugin
plugin = semantic_kernel_adapter.import_plugin("math", sdk_plugin)

# Create and execute plans
plan = semantic_kernel_adapter.create_plan("Research AI trends")
result = await semantic_kernel_adapter.execute_plan(plan)
```

**Architecture:**

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    SEMANTIC KERNEL INTEGRATION                           │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│    Semantic Kernel                         PyAgent                       │
│   ┌─────────────────┐                 ┌─────────────────┐               │
│   │                 │                 │                 │               │
│   │  ┌───────────┐  │  ◄──────────►   │  ┌───────────┐  │               │
│   │  │  Kernel   │  │   Adapter       │  │  Agent    │  │               │
│   │  └───────────┘  │                 │  └───────────┘  │               │
│   │                 │                 │                 │               │
│   │  ┌───────────┐  │  ◄──────────►   │  ┌───────────┐  │               │
│   │  │  Plugin   │  │   Adapter       │  │  Skill    │  │               │
│   │  └───────────┘  │                 │  └───────────┘  │               │
│   │                 │                 │                 │               │
│   │  ┌───────────┐  │  ◄──────────►   │  ┌───────────┐  │               │
│   │  │  Planner  │  │   Adapter       │  │Orchestrate│  │               │
│   │  └───────────┘  │                 │  └───────────┘  │               │
│   │                 │                 │                 │               │
│   └─────────────────┘                 └─────────────────┘               │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘
```

### 2.3 Vector Database Connectors

**Purpose:** Unified interface for vector stores to power RAG operations.

```python
from pyagent.integrations import vector_db

# Azure AI Search
store = vector_db.connect("azure_ai_search",
    endpoint="https://my-search.search.windows.net",
    index="documents"
)

# ChromaDB (local)
store = vector_db.connect("chroma", path="./data")

# Pinecone
store = vector_db.connect("pinecone",
    api_key="...",
    environment="us-west1-gcp"
)

# Use with RAG
from pyagent import rag
indexed = rag.index(documents, store=store)
answer = indexed.ask("What is the main conclusion?")
```

**Supported Backends:**

| Backend | Type | Production Ready | Notes |
|---------|------|-----------------|-------|
| Azure AI Search | Cloud | ✅ | Enterprise, RBAC |
| Pinecone | Cloud | ✅ | Serverless option |
| Weaviate | Cloud/Self | ✅ | GraphQL API |
| Chroma | Local | ✅ | Development |
| FAISS | Local | ✅ | CPU/GPU |
| Qdrant | Cloud/Self | ✅ | Rust-based |
| PostgreSQL pgvector | Self | ✅ | SQL integration |

---

## 3. MCP (Model Context Protocol) Integration

### 3.1 MCP Architecture

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    MODEL CONTEXT PROTOCOL (MCP)                          │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│   ┌─────────────────────────────────────────────────────────────────┐   │
│   │                        MCP SERVER                                │   │
│   │                                                                   │   │
│   │   ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐        │   │
│   │   │  Tool 1  │  │  Tool 2  │  │  Tool 3  │  │  Tool N  │        │   │
│   │   └──────────┘  └──────────┘  └──────────┘  └──────────┘        │   │
│   │                                                                   │   │
│   │        ▲              ▲              ▲              ▲            │   │
│   │        │              │              │              │            │   │
│   │   └────┴──────────────┴──────────────┴──────────────┴────┘      │   │
│   │                              │                                    │   │
│   │                    MCP Protocol (JSON-RPC)                       │   │
│   │                              │                                    │   │
│   └──────────────────────────────┼───────────────────────────────────┘   │
│                                  │                                       │
│                                  ▼                                       │
│   ┌─────────────────────────────────────────────────────────────────┐   │
│   │                        MCP CLIENT                                │   │
│   │                                                                   │   │
│   │   ┌──────────────────────────────────────────────────────────┐  │   │
│   │   │  mcp.connect("npx @modelcontextprotocol/server-github")  │  │   │
│   │   │  mcp.connect("uvx mcp-server-fetch")                      │  │   │
│   │   │  mcp.connect("python my_server.py")                       │  │   │
│   │   └──────────────────────────────────────────────────────────┘  │   │
│   │                                                                   │   │
│   └─────────────────────────────────────────────────────────────────┘   │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘
```

### 3.2 Creating MCP Tools

```python
from pyagent import mcp

@mcp.tool("calculate")
def calculate(expression: str) -> float:
    """Evaluate a mathematical expression."""
    return eval(expression)

@mcp.tool("search_docs")
def search_docs(query: str, limit: int = 5) -> list:
    """Search documentation for relevant content."""
    # Implementation
    return results

# Create server
server = mcp.server("my-tools", tools=[calculate, search_docs])
server.start()
```

### 3.3 Connecting to MCP Servers

```python
from pyagent import mcp, agent

# Connect to external MCP servers
github_tools = mcp.connect("npx @modelcontextprotocol/server-github")
fetch_tools = mcp.connect("uvx mcp-server-fetch")

# Create agent with MCP tools
my_agent = agent(
    "You are a research assistant",
    tools=github_tools + fetch_tools
)
```

---

## 4. Cloud Platform Integrations

### 4.1 Azure Integration

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    AZURE INTEGRATION ARCHITECTURE                        │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│   ┌─────────────────────────────────────────────────────────────────┐   │
│   │                    AZURE ACTIVE DIRECTORY                        │   │
│   │                                                                   │   │
│   │   DefaultAzureCredential ──► Access Token ──► RBAC Validation   │   │
│   │                                                                   │   │
│   └─────────────────────────────┬───────────────────────────────────┘   │
│                                 │                                        │
│         ┌───────────────────────┼───────────────────────┐               │
│         │                       │                       │               │
│         ▼                       ▼                       ▼               │
│   ┌───────────────┐     ┌───────────────┐     ┌───────────────┐        │
│   │ Azure OpenAI  │     │ Azure AI      │     │ Azure Key     │        │
│   │               │     │ Search        │     │ Vault         │        │
│   │ • gpt-4o      │     │               │     │               │        │
│   │ • gpt-4o-mini │     │ • Vector      │     │ • Secrets     │        │
│   │ • embeddings  │     │ • Semantic    │     │ • Keys        │        │
│   └───────────────┘     └───────────────┘     └───────────────┘        │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘
```

### 4.2 Multi-Cloud Support (Roadmap)

| Cloud | Service | Status | Notes |
|-------|---------|--------|-------|
| **Azure** | Azure OpenAI | ✅ Ready | Default provider |
| **Azure** | AI Search | ✅ Ready | Vector store |
| **Azure** | Key Vault | ✅ Ready | Secrets |
| **AWS** | Bedrock | 🔄 Planned | Q2 2026 |
| **AWS** | Kendra | 🔄 Planned | Q2 2026 |
| **GCP** | Vertex AI | 🔄 Planned | Q3 2026 |
| **GCP** | Vector Search | 🔄 Planned | Q3 2026 |

---

## 5. Development Tooling

### 5.1 CLI Tools

```bash
# Initialize new PyAgent project
pyagent init my-project

# Run tests
pyagent test

# Start MCP server
pyagent mcp serve ./tools

# Deploy to Azure
pyagent deploy azure --config deployment.yaml

# Generate documentation
pyagent docs generate
```

### 5.2 VS Code Extension (Planned)

Features:
- IntelliSense for PyAgent APIs
- Agent preview panel
- Trace visualization
- MCP server management
- Azure deployment integration

### 5.3 Testing Utilities

```python
from pyagent.testing import MockLLM, TestAgent

# Mock LLM for testing
mock = MockLLM(responses={
    "What is AI?": "AI is artificial intelligence.",
    "*weather*": "The weather is sunny."
})

# Test agent behavior
with TestAgent(my_agent, llm=mock) as test:
    response = test.ask("What is AI?")
    assert "artificial intelligence" in response
    test.assert_no_errors()
```

---

## 6. Monitoring & Observability

### 6.1 Tracing Architecture

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    OBSERVABILITY STACK                                   │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│   Application                                                            │
│   ┌─────────────────────────────────────────────────────────────────┐   │
│   │  from pyagent import trace                                       │   │
│   │  trace.enable()                                                  │   │
│   │  with trace.span("agent_run"):                                   │   │
│   │      result = agent("Hello")                                     │   │
│   └─────────────────────────────────┬───────────────────────────────┘   │
│                                     │                                    │
│                                     ▼                                    │
│   ┌─────────────────────────────────────────────────────────────────┐   │
│   │                    Trace Exporters                               │   │
│   │                                                                   │   │
│   │   ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐        │   │
│   │   │ Console  │  │ JSON File│  │  OTLP    │  │App Insight│        │   │
│   │   └──────────┘  └──────────┘  └──────────┘  └──────────┘        │   │
│   │                                                                   │   │
│   └─────────────────────────────────────────────────────────────────┘   │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘
```

### 6.2 Metrics

| Metric | Description | Type |
|--------|-------------|------|
| `pyagent_requests_total` | Total LLM requests | Counter |
| `pyagent_request_duration_seconds` | Request latency | Histogram |
| `pyagent_tokens_used` | Token consumption | Counter |
| `pyagent_guardrails_blocked` | Blocked requests | Counter |
| `pyagent_handoffs_total` | Agent handoffs | Counter |
| `pyagent_errors_total` | Error count | Counter |

---

## 7. Future Roadmap

### Q1 2026 (Current)
- ✅ LangChain adapter
- ✅ Semantic Kernel adapter
- ✅ Vector DB connectors
- ✅ MCP support
- 🔄 CLI tools

### Q2 2026
- AWS Bedrock provider
- Ollama provider
- VS Code extension
- Advanced caching

### Q3 2026
- Google Vertex provider
- Real-time streaming
- Voice integration
- Mobile SDK

### Q4 2026
- Edge deployment
- Federated learning
- Custom model fine-tuning
- Enterprise dashboard

---

*This document is maintained by the PyAgent Product Team.*
